import { AllowkeysDirective } from './allowkeys.directive';

describe('AllowkeysDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowkeysDirective();
    expect(directive).toBeTruthy();
  });
});
